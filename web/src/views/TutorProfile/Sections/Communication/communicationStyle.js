import { mlAuto, mrAuto } from "assets/jss/material-kit-pro-react.js";

const communicationStyle = (theme) => ({
  gridItem: {
    ...mlAuto,
    ...mrAuto,
  },
});

export default communicationStyle;
